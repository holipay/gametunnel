# Plan: 拆分 server.go

## 目标
将 `internal/server/server.go`（1023行，32个函数）拆分为 5 个职责清晰的文件。

## 拆分方案

### 1. `server.go` — 核心服务器逻辑（保留）
保留内容：
- Server 结构体定义
- Config 结构体
- New()、Run()、Close()
- worker()、handlePacket()、handlePacketMultiRoom()
- sendChecked()、sendKick()、sendKickCode()
- bwCleanupLoop()

预计 ~500 行

### 2. `client.go` — 客户端状态（新建）
从 server.go 移出：
- Client 结构体定义（line 85-104）
- GetLastSeen()、SetLastSeen()、PingStats()
- authState 类型和常量
- pingHistorySize 常量

预计 ~120 行

### 3. `pool.go` — 缓冲池管理（新建）
从 server.go 移出：
- pktPools 变量（line 29-34）
- pktPoolGet() 函数
- pktPoolPut() 函数

预计 ~70 行

### 4. `keepalive_server.go` — 服务器保活/NAT（新建）
从 server.go 移出：
- keepaliveLoop()（line 749-860）
- handleNATProbe()（line 861-885）
- handleRebind()（line 887-982）
- sendRebindAck()（line 983-988）

预计 ~250 行

### 5. `subnet.go` — 子网分配（新建）
从 server.go 移出：
- allocateSubnet()（line 602-644）
- getLocalSubnets()（line 646-673）
- subnetOverlapsAny()（line 675-691）

预计 ~90 行

## 执行步骤

1. 创建 `client.go`，移出 Client 结构体和相关方法
2. 创建 `pool.go`，移出 pktPool 相关代码
3. 创建 `keepalive_server.go`，移出 keepaliveLoop/NAT/rebind
4. 创建 `subnet.go`，移出子网分配逻辑
5. 清理 server.go 中的移出代码和多余的 import
6. 运行构建和测试验证

## 验证
- `go build ./...`
- `go vet ./...`
- `go test -race ./internal/... -count=1 -timeout 120s`
