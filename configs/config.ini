# GameTunnel 配置文件
# 服务器地址（必填，如 1.2.3.4 或 2408::1）
server=
# 服务器端口（默认 4700）
port=4700
# 玩家名称（默认：计算机名）
name=
# 房间 ID（默认：default）
room=default
# 密码（无密码留空）
password=
# 语言（zh 中文 / en 英文）
lang=zh
# 隧道 MTU（576-9000，默认 1280）
mtu=1280
# 日志文件路径（留空 = 仅输出到控制台）
#log-file=
# 详细日志（true / false）
#verbose=false

