# GameTunnel Configuration
# Server address (required, e.g. 1.2.3.4:4700)
server=
# Player name (default: computer name)
name=
# Room ID (default: default)
room=default
# Password (leave empty if none)
password=
# Language (zh or en)
lang=zh
